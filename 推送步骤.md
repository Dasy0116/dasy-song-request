# Dasy独狼点歌台 · 更新歌单推送步骤

> 更新歌单后按以下步骤操作，网站会自动重新部署，无需更换网址。

## 一、修改歌单

打开 `public/songs.json`，在数组中追加一首歌，格式参考已有歌曲：

```json
{
  "id": 103,
  "title": "歌名",
  "artist": "歌手",
  "language": "日语",
  "genre": "术曲",
  "firstLetter": "歌名拼音/罗马音首字母（大写）",
  "isPaid": false,
  "hasClip": false,
  "remark": "-",
  "status": "available"
}
```

字段说明：
- `id`：唯一编号，取当前最大 id + 1
- `language`：国语 / 日语 / 英语 / 韩语 / 其他
- `genre`：流行 / 摇滚 / ACG / 术曲 / 民谣 / JPOP / 古风
- `firstLetter`：用于首字母筛选，**大写英文字母**（中文歌取拼音首字母，外文歌取罗马音/原文首字母）
- `isPaid`：是否付费歌（true/false）
- `hasClip`：是否有歌切（true/false）
- `status`：available（可点）/ full（满员）/ closed（关闭）

## 二、提交并推送

在项目根目录 `c:\Users\Czy\Desktop\点歌` 下打开终端，依次执行：

```bash
git add public/songs.json
git commit -m "feat: 添加新歌 歌名"
git push
```

## 三、等待自动部署

推送后 GitHub Actions 会自动触发构建并部署到以下网址（约 1-2 分钟）：

- 主站（Netlify，国内访问快）：https://dasy-song-0116.netlify.app/
- 备用（GitHub Pages）：https://dasy0116.github.io/dasy-song-request/

可在 GitHub 仓库的 **Actions** 页面查看部署进度：
https://github.com/Dasy0116/dasy-song-request/actions

## 四、验证

刷新上面的网址，确认新歌已出现在歌单中即可。

---

## 备注

- 后台管理地址：https://dasy-song-0116.netlify.app/#/admin（密码 `Dasy0116`），可查看粉丝点歌记录
- 只修改 `songs.json` 即可，无需改动其他文件
- 若推送失败提示 `non-fast-forward`，先执行 `git pull` 再 `git push`
